spec:
  work_dir: "s3://cubed-mgroverwx-temp"
  allowed_mem: "2GB"
  executor_name: "ray"
  executor_options:
    ray_init:
      runtime_env:
        pip: ["cubed", "s3fs"]
      ignore_reinit_error: True
